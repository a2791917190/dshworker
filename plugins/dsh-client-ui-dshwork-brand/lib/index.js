/**
 * DSHwork brand plugin — node half. Pure UI surface: the empty apply exists so
 * the plugin appears in the host Loader (lifecycle follows the host); the browser
 * half ships via exports["./client"] (declared through package.json dsh.client).
 */
export function apply() {}
